# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["NPM_CONFIG_PRODUCTION=false npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"